# Handover, May 14 2026 - End of evening session

## Status

Productive evening. Track A (AI cost meter) shipped. Track B (Schedule rebuild Phase 1 + Phase 2) shipped. Marketing pillar updates on Home and Features shipped. 14 commits since 5:15pm ET.

## Commits in order (most recent last)

| # | Commit | What |
|---|---|---|
| 1 | `87909e24` | Founder playbook: commodity-surface + intelligence-layer principle |
| 2 | `603cb38e` | Founder playbook: Fill This Gap match scoring + SMS templates |
| 3 | `efe9f88b` | Founder playbook: Smart Booking pillar + SOAP AI approach + handover doc |
| 4 | `84ef4e6c` | AI cost meter on /founder dashboard |
| 5 | `c8210d2d` | SmartBookingRail wired into TimelineView 2-col layout |
| 6 | `1d676fac` | SOAP intelligence pipeline (table + edge function + auto-trigger) |
| 7 | `bff07511` | Up-Next briefing wired to real bookings + session_intelligence |
| 8 | `49bac511` | Body Load + Revenue cards wired to real data |
| 9 | `e22820af` | Footer cards hidden on Schedule; full-day timeline; FillGap real wire |
| 10 | `d93aea8f` | Open-time labels; monthly revenue framing; honest fill-gap |
| 11 | `2c71d112` | Mobile fix: fluid carousel cards + minWidth 0 chain |
| 12 | `1c8f176c` | Mobile: Body Load + Revenue 2-up row to cut vertical scroll |
| 13 | `d209f662` | Marketing pillar: Smart Booking framing on Home + Features |

## What's live now

**Schedule tab (Today view):**
- Left rail: Up-Next briefing carousel (4 upcoming clients, 3 points each, arrows + swipe), Body Load meter (real service-keyword math), May Revenue card (month-to-date + 4-week sparkline), Fill This Gap (real gap + best-match client + pre-drafted SMS) when conditions met
- Right calendar pane: full working day 8 AM to 7 PM, hourly grid, status-colored appointment blocks, now-line, soft amber Open labels on long open stretches, urgent fill-this-gap callouts on short fillable stretches
- Desktop 2-column grid, mobile single-column with Body Load + Revenue as 2-up compact row to save vertical scroll
- Footer cards (Total Clients, Sessions, Plan) hidden on Schedule view, still show on Clients, Sessions, etc.

**Founder dashboard (/founder):**
- AI cost meter card between stats strip and filter bar
- Three big numbers: today, this month, lifetime USD cost
- Breakdown bars by caller + purpose
- Auto-refreshes every 30s
- Currently shows zero until HK runs the ai_call_log migration

**Marketing pages:**
- Home > Day-of-Session ribbon now leads with Smart Booking pillar language
- Features > Schedule section now uses 'Smart Booking' eyebrow and 'The Right Client. The Right Slot. One Tap.' h2
- WhyBodyMap already had Smart Booking framing (no changes needed this session)

## HK must run two migrations before counters and AI extraction work

Files in the repo:
- `supabase/migrations/2026-05-14-ai-call-log.sql`
- `supabase/migrations/2026-05-14-session-intelligence.sql`

Run both in the Supabase SQL editor. Each is a single CREATE TABLE plus indexes plus RLS policies. Idempotent (CREATE IF NOT EXISTS), safe to re-run.

Once those run:
1. /founder AI cost meter starts populating as PracticeIQ or founder-chat calls happen
2. SOAP saves trigger background AI extraction; the briefing card on Schedule starts showing real points pulled by Haiku for each upcoming client's most recent completed session

## What is NOT done

- **Reverse Google Calendar sync verification.** From earlier today: forward push to Google Calendar was fixed (column-name bug, commit d27d1ed8) but never re-tested with browser devtools open. Test status: pending HK.
- **Full Fill This Gap match scoring.** Current implementation finds the first today gap >= 60 min between consecutive bookings and ranks lapsed clients (4+ visits, more than 30 days lapsed) by day-of-week match + phone-on-file bonus. The full playbook algorithm (cadence_match + time_of_day_match + day_of_week_match + recent_contact_signal + service_fit - no_show_rate) is documented but only partially implemented. v2 next session.
- **Therapist timezone column.** Forward calendar push currently uses UTC placeholder for the event timezone. Events will display 4-7 hrs off from the therapist's local time until therapists.timezone column is added and used in google-calendar-push. Pending.

## Phase 2 priority queue for next session

1. Run the migrations (HK) - unlocks everything below
2. Verify the briefing card pulls real SOAP intelligence after a SOAP save
3. Full Fill This Gap match scoring algorithm per playbook formula
4. Therapist timezone column for accurate Google Calendar times
5. Verify Postgres app.settings.service_role_key is set for reverse cron
6. WhyBodyMap visual refresh if needed
7. Address screenshot feedback from HK on the Schedule rebuild

## Critical rules honored this session

- No em dashes anywhere in commits (verified before each push)
- 'MyBodyMap' not 'BodyMap' in user-facing text
- No 'AI-powered' in customer copy
- React hooks imported individually
- No dropdowns in therapist-facing UI
- No window.confirm
- No secrets in repo
- Edge functions auto-deploy via GitHub Actions
- Single founder playbook (docs/MARKETING_MYBODYMAP.md), no doc proliferation
- Defaults work for 95 percent out of the box, no required settings
- Familiar surfaces, intelligence layer using proprietary data, zero new inputs

## Files touched this session

Migrations (need HK to run):
- supabase/migrations/2026-05-14-ai-call-log.sql
- supabase/migrations/2026-05-14-session-intelligence.sql

Edge functions (auto-deployed):
- supabase/functions/_shared/ai_cost.ts (new)
- supabase/functions/extract-session-intelligence/index.ts (new)
- supabase/functions/bodymap-ai/index.ts (patched, 3 call sites log)
- supabase/functions/founder-chat/index.ts (patched, logs Opus calls)

Frontend (Vercel auto-deployed):
- src/components/AiCostCard.jsx (new)
- src/components/schedule/SmartBookingRail.jsx (new, ~1230 lines)
- src/components/FounderDashboard.js (mounts AiCostCard)
- src/components/ScheduleDashboard.js (wires SmartBookingRail; full-day timeline)
- src/components/SessionDetail.js (fires extraction on save + complete)
- src/pages/Dashboard.js (hides footer cards on Schedule)
- src/pages/Home.jsx (Day-of-Session Smart Booking framing)
- src/pages/Features.jsx (Schedule section Smart Booking framing)

Docs:
- docs/MARKETING_MYBODYMAP.md (commodity-surface principle + Fill Gap formula + Smart Booking pillar + SOAP AI approach)
- docs/HANDOVER_2026-05-14.md (regenerated, this file)
