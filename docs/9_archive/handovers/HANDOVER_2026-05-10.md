# HANDOVER 2026-05-10 → next session

This document captures everything needed for a fresh Claude
session to pick up MyBodyMap work where May 10 2026 left off,
without re-explaining context.

---

## How to use this

In the new thread, paste:

> "Please read docs/HANDOVER_2026-05-10.md and docs/BLOCK_PLAN.md
> in the bodymap repo before doing anything else. Then ask me
> which item I want to tackle first."

That should orient the new session to the active state of work
without re-litigating decisions. Attach screenshots as needed.

---

## Repo + access

- Repo: `https://github.com/bodymapapp/bodymap` (not `cap.git`,
  that typo keeps appearing in URLs — always retry to bodymap.git)
- Working branch: `main`
- GitHub PAT for pushing: stored in HK's memory, not in repo
- Founder login for testing: `bodymapdemo@gmail.com`
- Test therapist custom_url: `healinghands`
- Supabase project ref: `rmnqfrljoknmellbnpiy`
- Supabase dashboard: search for project by ref above
- Vercel deploy token: stored in HK's memory, not checked into repo
- Resend API key: stored in HK's memory, not checked into repo
- Cloudflare Zone ID: stored in HK's memory, not checked into repo
- GitHub PAT for the bodymap repo: stored in HK's memory, not
  checked into repo. Claude receives all of the above as
  conversation memory at the start of each session.

Edge functions auto-deploy on push to main via
`.github/workflows/deploy-edge-functions.yml`. Never ask HK to
paste code into the Supabase dashboard for edge function code.

---

## What shipped on May 10 2026 (and what is broken)

### Working / verified

- Google Calendar sync foundation (commits `ed81097d`, `1063c227`,
  `d143dff5`, `d193b8d9`, `cb52320f`)
  - OAuth flow, schema, edge functions, settings UI, external
    event rendering on therapist schedule, slot blocking on
    booking page. Connection confirmed working by HK.
  - Verification submission tabled in BLOCK_PLAN section 1.

- Smart Scheduling demo redesign (`67caa39b`)
  - Three-mode timeline (Off / Soft / Hard) with three booked
    bookings, time labels under offered dots, strikethrough
    "12 PM" text for hidden slots. Lives in
    `src/components/demos/SmartScheduleDemo.jsx`.

- "Custom days per service" rename (`67caa39b`)
  - Feature was "Service days," now "Custom days per service"
    across featuresData, Home, ServiceDaysDemo, FEATURES_TAXONOMY.

- Disclosure-row pattern across all of Settings (earlier
  commits today) including the Google Calendar disclosure row.

### Shipped but unverified by HK

- Editable intake migration (`7aaecf6f` + `339cfcac`)
  - Migration applied successfully by HK.
  - Schema: sessions has front_focus_therapist,
    back_focus_therapist, front_avoid_therapist,
    back_avoid_therapist, front_pct, top_pct, middle_pct,
    bottom_pct. New table intake_edits with RLS.

- Therapist intake editor on SessionDetail (`339cfcac`)
  - Code is at src/components/SessionDetail.js around line 432.
  - HK opened the URL
    https://www.mybodymap.app/dashboard/clients/1565eac6-ceff-4e81-a038-82fe5e8299c6/sessions/3b07f57d-7e94-432c-a2b5-779c14faad1b
    and did NOT see the Edit button.
  - **Diagnostic needed first thing in the new session.** See
    BLOCK_PLAN entry 3 for the troubleshooting list.

- Focus distribution sliders on intake (`339cfcac`)
  - Code is at src/components/FocusDistribution.jsx, wired into
    Demo.jsx back-body screen at line ~5000.
  - HK confirmed they appear but the design was rejected.
    Specific feedback captured in BLOCK_PLAN entry 4.

---

## Lindsey's 11-item list, where it stands

From the earlier "Lindsey email" feature list:

| # | Item | Status |
|---|---|---|
| 1 | Cancel/reschedule/refund | Pending. Dedicated 5-7 day sprint planned. Has its own design doc needed (Item 37). |
| 2 | Tips + pay-in-full | Done. Shipped earlier in this multi-day session. |
| 3 | Follow-up email for new clients | Done (Outreach quick-send v1). |
| 4 | Different calendar per service | Done. Schema + BookingPage filter + Settings day pills + marketing surfaces. |
| 5 | Booking lead-time | Done. |
| 6 | (paired with #1, Item 37) | Pending with #1. |
| 7 | Disallow gaps in day | Done as Smart Scheduling 3-way control. |
| 8 | Manual time slots | Pending. Therapist adds one-off slots outside her regular schedule. |
| 9 | Service-duration-aware slots | Done. Already existed. |
| 10 | Calendar sync iCal/Google | Cal.com done. Google ~80% done. Verification tabled. |
| 11 | Bidirectional intake editing | Functional code shipped but HK unable to verify Edit button. Design rework needed regardless. |

---

## Top priorities for the next session

In order:

### Priority 1: Edit button diagnostic

Investigate why HK does not see the Edit button at
https://www.mybodymap.app/dashboard/clients/1565eac6-ceff-4e81-a038-82fe5e8299c6/sessions/3b07f57d-7e94-432c-a2b5-779c14faad1b
after commit `339cfcac` shipped.

Steps in order:
1. Ask HK to hard-refresh in incognito and confirm whether the
   button is still missing. Vercel cache could explain it.
2. If still missing, ask HK to open browser devtools console
   on that URL and run:
   `document.body.innerText.includes('✏️ Edit')`
   - true: button is in DOM, CSS hiding it
   - false: render bug, my edit did not land
3. View the current state of
   `src/components/SessionDetail.js` around line 430 to verify
   the Edit button code exists.
4. If a screenshot would help, ask HK to upload one of the
   client preferences card on SessionDetail.

### Priority 2: Card-on-file detection bug (BLOCK_PLAN entry 5)

Customer-facing. HK has booked the same client (their own
test booking) 5+ times and the booking page still prompts for
a card every time. The booking page already has
`isRepeatClient` detection (line 767 of BookingPage.js) but
the skip-card-prompt logic isn't firing.

Two surfaces to fix:
- Booking page: skip card prompt when client has
  payment_method_id, show "Card on file ending 4242" with an
  option to use a different card.
- Client detail / client card: show a "Card on file" indicator
  with last 4 digits when payment_method_id is set.

### Priority 3: Slider redesign + intake flow polish (BLOCK_PLAN entry 4)

HK's specific feedback on the FocusDistribution sliders:
- Move to next to body image on top of the page, not below
- Make the percentage numbers EDITABLE (type on top of "20%")
- Use dotted lines connecting sliders to body zones
- Add back button to Preferences screen so client can return
  to body-map screens
- Add auto-save throughout the intake form (localStorage
  draft restored on page load)

This is a real design rework session. ~4-6 hours.

### Priority 4: Body SVG C/T zone editing (BLOCK_PLAN entry 2)

Therapist edits body zones on SessionDetail with C/T badges
distinguishing client-marked from therapist-marked zones.
Schema columns front_focus_therapist etc already exist. UI
not yet built. ~2-3 hours.

### Priority 5: Google verification (BLOCK_PLAN entry 1)

When privacy policy + terms are live at mybodymap.app/privacy
and /terms, submit for Google OAuth app verification. All
materials (justification, video script, additional info text)
are already drafted in BLOCK_PLAN entry 1. HK records video,
uploads to YouTube unlisted, pastes into form, submits.

---

## Key operating rules (preserved across sessions)

These are HK preferences that should never be violated:

1. NEVER use em dashes anywhere (code, comments, UI, marketing,
   emails). Use commas, periods, or rephrase.
2. ALWAYS use "MyBodyMap" in user-facing text. Never just
   "BodyMap" (acceptable only for legal entity names).
3. React hooks imported individually: `import { useState }`
   never `React.useState`. Vercel production builds fail
   silently on `React.useState`.
4. Mobile-first design. Desktop is an enhancement layer.
5. Never take shortcuts. Find root cause and fix it. Flag any
   temporary workaround explicitly.
6. Sign emails as "Joy / MyBodyMap Team" not HK.
7. Never ask HK to paste code into Supabase dashboard for edge
   functions. They auto-deploy via GitHub Actions.
8. Google Search Console is already verified for mybodymap.app.
   Don't assume SEO infra needs setup.
9. NEVER use dropdowns for therapist-facing UI. Use the
   InlineSaveNumberInput pattern (`src/components/InlineSaveNumberInput.jsx`).
10. NEVER use window.confirm dialogs. Use soft inline-undo
    banners.
11. Always grep for table names before writing migrations
    (outreach_sends collision near-miss happened).
12. Always run information_schema diagnostic before writing
    inserts to a table.
13. Bookings linked to clients by email AND client_id.
14. Disclosure-row pattern (`src/components/DisclosureRow.jsx`)
    for all Settings panels. iOS-style, one expanded at a time.
15. Per-service availability is days-only, not hours. Custom
    hours per service deferred.
16. "Smart Scheduling" is the external marketing name.
    "Efficient scheduling" is acceptable internally as it
    describes the mechanic.
17. Stop padding time estimates. Quote what the work actually
    takes. HK called this out as a real pattern.
18. For files >15KB use create_file not str_replace cascades.
19. Always explicit `git add` paths.
20. When a tool result is followed by `cap.git` errors,
    retry the push to `bodymap.git`. Persistent typo.

---

## Where everything lives

### Recently-shipped components

- `src/components/DisclosureRow.jsx` — iOS-style disclosure row primitive
- `src/components/FocusDistribution.jsx` — focus distribution sliders (needs redesign)
- `src/components/demos/SmartScheduleDemo.jsx` — Home page demo (current version)
- `src/components/demos/ServiceDaysDemo.jsx` — Home page demo
- `src/components/SessionDetail.js` — therapist session view (Edit button at ~line 432)
- `src/components/ScheduleDashboard.js` — therapist schedule view (renders external Google events as lavender)
- `src/lib/focusDistribution.js` — zone-to-band mapping + auto-derive helpers
- `src/lib/waiver.js` — default waiver text (now includes therapist-edit consent)

### Edge functions

- `supabase/functions/google-calendar-connect/index.ts` — builds OAuth URL server-side
- `supabase/functions/google-calendar-callback/index.ts` — handles OAuth redirect, saves tokens
- `supabase/functions/google-calendar-push/index.ts` — forward sync (booking → Google event)
- `supabase/functions/google-calendar-sync/index.ts` — reverse sync (Google events → external_calendar_events)

### Pages

- `src/pages/Demo.jsx` — the master intake form (5111 lines, careful)
- `src/pages/ClientIntake.js` — public intake landing page that wraps Demo.jsx
- `src/pages/BookingPage.js` — public booking page (card-on-file logic bug lives here)
- `src/pages/Dashboard.js` — therapist dashboard (Settings, Clients, etc)

### Migrations applied today

All in `docs/migrations/`:
- `2026-05-10-efficient-scheduling.sql` — applied
- `2026-05-10-tips-and-pay-in-full.sql` — applied
- `2026-05-10-per-service-availability.sql` — applied
- `2026-05-10-google-calendar-sync.sql` — applied
- `2026-05-10-google-calendar-cron-setup.sql` — NOT applied (cron disabled for now)
- `2026-05-10-editable-intake.sql` — applied

### Docs

- `FEATURES_TAXONOMY.md` — feature catalog with taxonomy IDs
- `docs/BLOCK_PLAN.md` — blocked tasks with materials
- `docs/HANDOVER_2026-05-10.md` — this document

---

## Final note on tone

HK pushes back hard when responses pad time estimates or
overclaim certainty. Keep responses direct, honest about what
is and is not known. If something is broken or uncertain, say
so plainly. Disagreement is welcome when it is on the merits.
Always find the root cause before patching the symptom.

End of handover.
