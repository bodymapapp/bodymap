# Handover: May 15 to May 16 2026 session

**Session end: late May 16 2026.** HK is exhausted, going to sleep. We finished a very long Stripe-focused session that fixed real bugs but also surfaced one question worth investigating tomorrow.

---

## What got shipped this session

In rough order:

1. **Client agreement: rebuilt as inline panel + searchable client rows** (`6420bf5f`). Killed the centered modal, killed the dropdown, fixed the `clients.full_name` vs `clients.name` column bug.
2. **Send-for-signature: short URLs, automated emails, editable message, OG preview cards** (`ac6ee0e5`). Five real fixes in one push.
3. **Services 2.1: friendly Remove + inline confirm** (`90eeb3a2`). Replaced the bare red x with an outlined Remove button + amber confirm bar.
4. **Client profile: Agreement section** (`9eee94c0`). Surfaces signed agreement state (signer name, datetime, snapshot of agreed text) on the client profile. Plus fixed the Vercel OG preview routing.
5. **Stripe Connect: fix the resume loop** (`dc9c58b9`). New `resume_onboarding` action, new `diagnose` action, RefreshState shows real missing requirements with plain-English translations, Settings page has three states.
6. **Stripe Debug page + auto-resume on refresh_url + Joy persona purge** (`57395d3a`). Debug page at /founder/stripe-debug initially, refresh_url now auto-mints fresh Account Link and redirects without UI step. Joy persona purged from HelpWidget, FounderDashboard templates, StripeConnect, Help.jsx, can-i-use-my-bank.md.
7. **Stripe: stop creating duplicate accounts** (`6b88382f`). The architectural fix. `get_oauth_url` now pre-checks for existing enabled accounts matching the therapist email before minting new ones. Added `stripe_account_ready_at` migration (column was referenced but didn't exist). Added `list_platform_accounts` and `attach_account` actions. Debug page got a Platform accounts table with one-click Attach.
8. **Stripe Debug moved into FounderHub** (`9197d9d4`). Section #8 of /founder. Disconnect button removed from Settings (no more accidental disconnects).
9. **Standard Connect alongside Express** (`67455c66`). New `get_standard_oauth_url` and `complete_standard_oauth` actions. New `/dashboard/stripe-connect-standard` callback page. `stripe_account_type` column migration. Settings page two-path UI.
10. **Help article: Stripe option to pick** (`dfef9b0f`). Now 20 articles.
11. **Standard edge case foolproofed** (`71cdf64d`). Express fallback visible from start (not hidden in disclosure). Error page distinguishes "no account selected" (user cancelled / didn't see expected) from generic failure. Article expanded to cover platform-Express case explicitly.
12. **Docs: full Stripe Connect architecture in BILLING_STRATEGY + recovery in FOUNDER_RUNBOOK** (`be3cacab`). Comprehensive operational documentation.
13. **Help article: I do not see my transactions in Stripe** (`9d6aa23f`). Explains the test vs live mode confusion. 21 articles now.

---

## Open question to investigate tomorrow

HK said at end of session, just before sleep:

> "earlier I believe the daya gupta account was connected to my stripe in platform but now it is not"

Specifically: HK feels something changed about the relationship between the Daya Gupta connected account and the platform Stripe account during tonight's work. Possible interpretations:

1. **Misunderstanding about architecture**: HK might be conflating "connected to my platform Stripe account" (which is how Connect works for all therapists, always) with something else they remember from earlier (e.g., a previous Standard Connect setup that we did not actually have).

2. **A real disconnect**: enabling OAuth in Stripe Dashboard might have done something we don't fully understand. Unlikely but worth checking.

3. **The "Standard would have linked to my existing Stripe" expectation**: HK expected the Standard OAuth flow to surface accounts like Daya Gupta. As I explained late in the session, Daya Gupta is an Express account owned by the platform and is not OAuth-listable. HK might be processing this and want to revisit.

### How to investigate tomorrow

Start with these questions to HK before doing anything:

1. "When you say 'connected to my Stripe in platform earlier,' what did you see that made you believe that was the case? A specific UI element? A previous experience? Be as specific as you can."

2. "Right now, on `/founder` -> Stripe Debug -> Refresh, what does the 'Stripe says' panel show for your account?" (Expected: charges_enabled true, payouts_enabled true, details_submitted true.)

3. "On `https://dashboard.stripe.com/connect/accounts/acct_1TXWhfQvokGFD9FY` what does the top of the page show?" (Tonight HK confirmed: "Connected Accounts. Payouts active." That means everything is working.)

If all three confirm the status quo is correct, the question is conceptual / architectural rather than a bug. Walk HK through:

- The platform Stripe account and the Daya Gupta connected account are intentionally separate Stripe entities
- They have always been separate; this is how Stripe Connect works
- "Connected" means: the connected account ID is stamped on HK's therapist row AND Stripe says charges_enabled
- Customer payments flow to the connected account's bank
- The platform Stripe account exists only to manage Connect settings and platform-level operations

If something genuinely changed (bug case 2), the smoking gun would be: pull HK's row history from Supabase audit logs (if enabled) or recent updates to therapists.stripe_account_id. Investigate from there.

---

## HK's other concerns mentioned tonight

1. **Stripe platform account is in sandbox / unverified state.** HK can't view live mode in Stripe Dashboard because the platform account itself is unverified. **This does not affect customer payments.** HK needs to spend 15-20 min verifying the platform account at some point. Not urgent.

2. **HK does not see transactions in Stripe Dashboard.** Root cause: test/live mode toggle defaults to test. Help article 'where-are-my-stripe-transactions.md' shipped covering this. Once HK verifies the platform account and flips to live mode, transactions will be visible. Easier path: `/dashboard/billing` inside MyBodyMap shows them with client names.

3. **Candice's connection** was fixed earlier tonight by SQL update. Her row stamped with `acct_1TXXY1JAq6V9VAbg`, connected flag flipped true. She can receive payments.

---

## What HK should NOT touch before we talk tomorrow

- The Stripe Connect settings in Stripe Dashboard. They are configured correctly now (OAuth enabled, redirect URIs added).
- Their therapist row's stripe_account_id. It's stamped to `acct_1TXWhfQvokGFD9FY` correctly.
- The 30 orphan Express accounts in Connect dashboard. Inert. Leave them.
- The STRIPE_CLIENT_ID env var in Supabase. (Open question: did HK paste the live or test Client ID? Worth verifying tomorrow but not urgent.)

---

## What HK SHOULD do as soon as they wake up

1. Read this handover.
2. Open `/founder` -> Stripe Debug -> Refresh. Confirm "Stripe says" shows all three booleans true.
3. Open `/dashboard/billing` and see if any historical transactions show.
4. Open the dashboard.stripe.com/connect/accounts/acct_1TXWhfQvokGFD9FY URL again, confirm "Payouts active."
5. Come back to the conversation with: (a) screenshots if any of the above show something unexpected, (b) the specific memory of what felt different about the Daya Gupta connection.

---

## State of the BLOCK_PLAN

Unchanged this session. Still queued:

1. Google OAuth verification (waiting on 5+ therapists)
2. Lindsey #11 polish (deferred)
3. Edit button on SessionDetail (diagnostic)
4. Slider redesign + intake auto-save (4-6 hrs)
5. Card-on-file detection for returning clients (TOP PRIORITY, ~1.5 hrs)
6. Optional client portal (waiting on 3+ requests)
7. Twilio onboarding friction (added this week)

Plus deferred from this session:
- **StatusStrip Agreement tile** (replace conditional Attention chip with permanent Agreement tile, ~75 min). All design decisions confirmed by HK: tile shows "Signed [date] · by [name]" when signed, "Not on file Tap to send" when unsigned. Tap-to-scroll on signed, tap-to-send on unsigned. Remove pendingIntake logic from attention. Deferred when Stripe became urgent.

---

## Three-sentence closing for tomorrow

Stripe is stable. Customer payments work for both HK (Daya Gupta) and Candice. The open question is conceptual, not a bug; investigate it first thing tomorrow before continuing any other work.
